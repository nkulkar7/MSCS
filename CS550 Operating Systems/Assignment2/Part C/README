Create the kernel module by compiling the mytime.c file using the Makefile.
The mytime.c and its Makefile are present in the directory kernel_module/.

Insert kernel module by executing following command:
sudo insmod mytime.ko

Give read permission to the device driver by executing following command:
sudo chmod a+r /dev/mytime

Compile user application (Display_mytime.c) present in the directory user_appl/ using Makefile present in that directory.

Execute user application by executing following command:
./Display_mytime

Remove kernel module by executing following command:
sudo rmmod mytime.ko

To view results of return value changed of module_init changed to -1 compile and insert the module present in directory module_init_-1/ and file mytime_module_init.c using the Makefile present in directory module_init_-1/



